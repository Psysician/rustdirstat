# crates/rds-gui/

egui/eframe GUI shell with directory picker, scanner integration, directory tree view, extension statistics, cushion-shaded treemap renderer, panel synchronization, drill-down navigation, duplicate file detection panel, delete-to-trash, open-in-file-manager, custom command actions, CSV/JSON export, config persistence, recent paths, settings dialog, toast notifications, scan error log, and max-nodes abort dialog.

## Files

| File | What | When to read |
| ---- | ---- | ------------ |
| `Cargo.toml` | Crate manifest; depends on `eframe`, `egui`, `egui-notify`, `streemap`, `crossbeam-channel`, `tracing`, `rds-core`, `rds-scanner`, `rfd`, `trash`, `open`, `serde`, `serde_json`, `csv`; dev-dep `tempfile`, `criterion`; `bench-internals` feature for exposing internals to benchmarks | Modifying GUI dependencies |
| `src/lib.rs` | `RustDirStatApp` with ScanPhase state machine, directory picker (rfd), scanner spawning, event drain loop (`DRAIN_BATCH_SIZE` = 5000), `tree_capacity_hint` for arena pre-allocation, 3-panel layout with tree view + treemap + ext stats, breadcrumb navigation, keyboard shortcuts (`Ctrl+O` browse, `Escape` cascade close/cancel, `F5` rescan, `Backspace` navigate up), theme application via `ThemePreference`, PendingDelete state, confirm_delete flow, delete confirmation dialog, CommandEditorState, custom_commands, ExportDialogState, freed_bytes tracking, format_bytes utility, config persistence via save callback, recent paths tracking/dropdown, settings dialog integration, `on_exit` save, `collect_config`/`save_config` helpers, toast notifications overlay, scan error log panel, max-nodes abort dialog; `bench-internals` feature re-exports `SubtreeStats`, `TreemapLayout`, `TreemapRect`, `MAX_DISPLAY_RECTS` | Modifying app state, scan lifecycle, layout, adding panel implementations, modifying delete action flow, modifying config persistence, modifying error display |
| `src/notifications.rs` | `Notifications` wrapper around `egui_notify::Toasts` with info/warning/error convenience methods and show overlay | Modifying toast notification behavior, adding notification types |
| `src/error_log.rs` | `ScanErrorLog` (capped Vec of path+error pairs with overflow counter), `show` panel rendering function | Modifying scan error display, changing error log capacity or rendering |
| `src/settings.rs` | `SettingsDialogState`, `show` (settings window with exclude patterns editor, sort order ComboBox, color scheme ComboBox, Apply/Cancel buttons) | Modifying settings dialog UI, adding new configuration options |
| `src/actions.rs` | `execute_delete` (trash + tombstone), `cleanup_duplicate_groups` (prune stale entries after deletion), `open_in_file_manager` (reveal file/dir in native file manager), `open_file_revealing` (platform-specific file select with D-Bus `FileManager1.ShowItems` on Linux before fallback), `execute_custom_command` (platform shell dispatch with {path} substitution) | Modifying file action logic |
| `src/command_editor.rs` | `show` (command editor window: inline editing of custom commands, add/remove controls, close button) | Modifying command editor UI |
| `src/export.rs` | `ExportFormat`, `ExportScope`, `ExportRecord`, `DuplicateExportRecord`, `ExportResult`, `ExportDialogState`, `export_tree` (DFS traversal + CSV/JSON write), `export_duplicates` (duplicate group flattening + CSV/JSON write), `default_filename`, `show_dialog` (export dialog window with format/scope ComboBox, rfd save dialog, result display) | Modifying export logic, adding export formats, modifying export dialog UI |
| `src/tree_view.rs` | `SubtreeStats` (cached subtree sizes/file counts, filters deleted nodes), `TreeViewState` (expanded nodes, selection sync, scroll-to), `expand_ancestors`, `sorted_children` (filters deleted), tree view rendering with right-click context menu (show/render_node) | Modifying tree view display, adding tree interactions, understanding size caching strategy, understanding auto-expand/scroll behavior |
| `src/ext_stats.rs` | `hsl_to_color32` (HSL→Color32 conversion), `show` (extension stats panel with stacked bar chart, scrollable Grid table, click-to-select extension filter) | Modifying extension statistics display, adding bar chart interactions, reusing hsl_to_color32 for treemap coloring |
| `src/treemap.rs` | `CushionCoeffs` (parabolic ridge accumulation, Lambertian intensity), `TreemapRect` (with `aggregated_count` field for merged buckets), `TreemapLayout` (cached layout with root tracking and cushion coefficients), `MAX_DISPLAY_RECTS` (50k cap), `compute_recursive` (recursive squarify with depth/cushion tracking and aggregation logic for excess items), `find_drill_target` (double-click navigation), `breadcrumb_chain` (ancestor path), `shade_color`, `grid_subdivisions`, `build_cushion_mesh`, `show` (cushion mesh render + flat fallback + extension dimming + tooltip + click selection + double-click drill-down + right-click context menu) | Modifying treemap rendering, tuning cushion constants, understanding drill-down navigation, understanding aggregation behavior |
| `src/duplicates.rs` | `show` (duplicates bottom panel with collapsible groups sorted by wasted space, selectable file paths with right-click context menu: Open in File Manager + Delete for cross-panel sync) | Modifying duplicate display, understanding panel layout ordering |
| `benches/treemap_bench.rs` | Criterion benchmarks for treemap layout compute, subtree stats compute, and aggregation at 1k-500k scale | Modifying treemap benchmarks, understanding layout performance |
