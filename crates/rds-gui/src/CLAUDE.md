# crates/rds-gui/src/

egui/eframe GUI implementation: app state machine, panels, interactions, and file actions.

## Files

| File | What | When to read |
| ---- | ---- | ------------ |
| `lib.rs` | `RustDirStatApp` (ScanPhase state machine, dir picker, scanner spawning, event drain loop with `DRAIN_BATCH_SIZE` = 5000, `tree_capacity_hint` for arena pre-allocation, WinDirStat-style layout (top: tree + extensions, bottom: treemap), keyboard shortcuts (`Ctrl+O` browse, `Escape` cascade close/cancel, `F5` rescan, `Backspace` navigate up), theme application via `ThemePreference`, treemap mesh cache (`TreemapMeshCache`), PendingDelete, confirm_delete, delete confirmation dialog, CommandEditorState, custom_commands, ExportDialogState, freed_bytes tracking, `format_bytes` utility, config persistence via save callback, recent paths tracking/dropdown, settings dialog integration, `on_exit` save, `collect_config`/`save_config` helpers, toast notifications overlay, scan error log panel, max-nodes abort dialog; `bench-internals` feature re-exports `SubtreeStats`, `TreemapLayout`, `TreemapRect`, `MAX_DISPLAY_RECTS`) | Modifying app state, scan lifecycle, layout, adding panel implementations, modifying delete action flow, modifying config persistence, modifying error display |
| `notifications.rs` | `Notifications` wrapper around `egui_notify::Toasts` with info/warning/error convenience methods and show overlay | Modifying toast notification behavior, adding notification types |
| `error_log.rs` | `ScanErrorLog` (capped Vec of path+error pairs with overflow counter), `show` panel rendering function | Modifying scan error display, changing error log capacity or rendering |
| `tree_view.rs` | `SubtreeStats` (cached subtree sizes/file counts, filters deleted nodes), `TreeViewState` (expanded nodes, selection sync, scroll-to), `expand_ancestors`, `sorted_children` (filters deleted), tree view rendering with right-click context menu | Modifying tree view display, adding tree interactions, understanding size caching strategy |
| `ext_stats.rs` | `hsl_to_color32` (HSL to Color32 conversion), `show` (extension stats panel with stacked bar chart, scrollable Grid table, click-to-select extension filter) | Modifying extension statistics display, reusing hsl_to_color32 for treemap coloring |
| `treemap.rs` | `CushionCoeffs`, `TreemapRect` (with `aggregated_count` field for merged buckets), `TreemapLayout` (cached layout with cushion coefficients), `TreemapMeshCache` (pre-built `Arc<egui::Mesh>` + flat rects), `MAX_DISPLAY_RECTS` (50k cap), `compute_recursive` (recursive squarify with aggregation), `build_mesh_cache` (builds cushion mesh once per layout change), `find_drill_target`, `breadcrumb_chain`, `show` (paints cached mesh + interaction handling) | Modifying treemap rendering, tuning cushion constants, understanding drill-down navigation, understanding aggregation behavior |
| `duplicates.rs` | `show` (duplicates bottom panel with collapsible groups sorted by wasted space, selectable file paths with right-click context menu: Open in File Manager + Delete) | Modifying duplicate display, understanding panel layout ordering |
| `actions.rs` | `execute_delete` (trash crate + arena tombstone), `cleanup_duplicate_groups` (prune stale entries after deletion), `open_in_file_manager` (reveal file/dir in native file manager), `open_file_revealing` (platform-specific file select with D-Bus `FileManager1.ShowItems` on Linux before fallback), `execute_custom_command` (platform shell dispatch with {path} substitution) | Modifying file action logic |
| `command_editor.rs` | `show` (command editor window: inline editing of custom commands, add/remove controls, close button) | Modifying command editor UI |
| `export.rs` | `ExportFormat`, `ExportScope`, `ExportRecord`, `DuplicateExportRecord`, `ExportResult`, `ExportDialogState`, `export_tree` (DFS traversal + CSV/JSON write), `export_duplicates` (duplicate group flattening + CSV/JSON write), `default_filename`, `show_dialog` (export dialog window with format/scope ComboBox, rfd save dialog, result display) | Modifying export logic, adding export formats, modifying export dialog UI |
| `settings.rs` | `SettingsDialogState`, `show` (settings window with exclude patterns editor, sort order ComboBox, color scheme ComboBox, Apply/Cancel buttons) | Modifying settings dialog UI, adding new configuration options |
